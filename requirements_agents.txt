# LangChain + LangGraph
langchain>=1.3.0
langchain-core>=1.4.0
langchain-google-genai>=4.2.2
langchain-groq>=1.1.0
langgraph>=1.2.0

# MCP client (HTTP/SSE transport to mcp-server container)
fastmcp>=0.1.0
httpx>=0.28.1

# Vector store + embeddings
# Pinned: the persisted store format is version-specific — a pre-1.0 store panics
# 1.0.x's Rust core on open. Keep in lockstep with whatever built data/chroma_db.
chromadb==1.0.15
sentence-transformers>=3.0.0

# Google Gen AI SDK
google-genai>=1.75.0

# Env management
python-dotenv>=1.0.0
