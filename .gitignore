data/chroma_db
models/
verify.py
.claude

# Secrets — never commit
.env

# SQLite app database (created at runtime / seeded)
data/app.db
data/app.db-wal
data/app.db-shm

# Python bytecode (all packages)
__pycache__/
*.pyc